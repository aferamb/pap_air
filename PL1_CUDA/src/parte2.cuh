#pragma once

/*
    parte2.cuh
    Fase 02: Evaluar los retrasos de llegada (ARR_DELAY) en función de un umbral dado, pero esta vez también considerando el número de cola (TAIL_NUM) del avión. \
    El programa imprimirá los valores que cumplen con el umbral, indicando si se trata de un retraso o un adelanto, junto con el número de cola del avión.
*/

void phase02(int threshold);
