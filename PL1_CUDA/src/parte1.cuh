#pragma once

/*
    parte1.cuh
    Fase 01: Evaluar los retrasos de salida (DEP_DELAY) en función de un umbral dado. El programa imprimirá los valores que cumplen con el umbral, indicando si se trata de un retraso o un adelanto.
*/

void phase01(int threshold);
